# Top-Level-Pins mit Major-Cap. Fuer reproduzierbare Production-
# Deployments waere ein vollstaendiger pip-compile-Lockfile besser
# (uv pip compile / pip-tools); dies hier ist der pragmatische
# Mittelweg.
paho-mqtt>=2.0.0,<3.0.0
sd-notify==0.1.0; sys_platform == 'linux'
psutil>=5.9.0,<8.0.0
click>=8.0.0,<9.0.0
pycaw>=20240210,<20270101; sys_platform == 'win32'
comtypes>=1.2.0,<2.0.0; sys_platform == 'win32'
pythonnet>=3.0.0,<4.0.0; sys_platform == 'win32'
